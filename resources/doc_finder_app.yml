resources:
  apps:
    doc_finder:
      name: doc-finder-${bundle.target}
      description: "Document search app for Integra LifeSciences"
      source_code_path: ../src/app

      resources:
        - name: sql-warehouse
          sql_warehouse:
            id: ${var.warehouse_id}
            permission: CAN_USE
